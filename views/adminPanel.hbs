<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Admin Panel</title>
</head>
<body>
    <div class="container mx-auto p-5">
        <h1 class="text-2xl font-bold">Admin Panel</h1>
        <table class="min-w-full border">
            <thead>
                <tr>
                    <th class="border p-2">Ticket No</th>
                    <th class="border p-2">User  Email</th>
                    <th class="border p-2">Description</th>
                    <th class="border p-2">Status</th>
                    <th class="border p-2">Actions</th>
                </tr>
            </thead>
            <tbody>
                {{#each tickets}}
                    <tr>
                        <td class="border p-2">{{this._id}}</td>
                        <td class="border p-2">{{this.userId.email}}</td>
                        <td class="border p-2">{{this.description}}</td>
                        <td class="border p-2">{{this.status}}</td>
                        <td class="border p-2">
                            <form action="/admin/tickets/{{this._id}}/status" method="POST" class="inline">
                                <select name="status" class="border p-1">
                                    <option value="Open">Open</option>
                                    <option value="In Progress">In Progress</option>
                                    <option value="Closed">Closed</option>
                                </select>
                                <button type="submit" class="btn">Update</button>
                            </form>
                            <form action="/admin/tickets/{{this._id}}/delete" method="POST" class="inline">
                                <button type="submit" class="btn text-red-500">Delete</button>
                            </form>
                        </td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</body>
</html>